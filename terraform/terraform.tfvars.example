# AWS Configuration
aws_region = "us-east-1"

# Project Settings
project_name = "secure-static-site"
environment  = "prod"

# EC2 Instance Configuration
instance_type     = "t3.micro"     # Free tier eligible
root_volume_size  = 20             # GB

# SSH Configuration
# Option 1: Create new key pair
create_key_pair     = true
ssh_public_key_path = "~/.ssh/id_rsa.pub"

# Option 2: Use existing AWS key pair (set create_key_pair = false)
# create_key_pair        = false
# existing_key_pair_name = "my-existing-key"

ssh_allowed_cidr = ["0.0.0.0/0"] # Change to your IP for better security: ["YOUR_IP/32"]

# Networking (leave empty to use default VPC/subnet)
vpc_id    = ""
subnet_id = ""

# Elastic IP
use_elastic_ip = true  # Set to true for persistent IP address

# Deployment User
deploy_user = "ubuntu"
